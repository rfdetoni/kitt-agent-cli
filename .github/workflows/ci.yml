name: CI

on:
  push:
    branches: [main]
  pull_request:

permissions:
  contents: read

concurrency:
  group: ci-${{ github.workflow }}-${{ github.event.pull_request.number || github.ref }}
  cancel-in-progress: true

jobs:
  test:
    name: python ${{ matrix.python-version }}
    strategy:
      fail-fast: false
      matrix:
        python-version: ["3.12", "3.13"]
    runs-on: ubuntu-latest
    timeout-minutes: 20
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          persist-credentials: false
      - uses: actions/setup-python@5fda3b95a4ea91299a34e894583c3862153e4b97 # v7.0.0
        with:
          python-version: ${{ matrix.python-version }}
          cache: pip
      - name: Install Agent development dependencies
        run: python -m pip install -e ".[dev]"
      - name: Compose Assistant runtime namespace
        run: >-
          python -m pip install --no-deps
          "git+https://github.com/rfdetoni/kitt-assistant.git@867519454ca97d0473bcefc515a39eb6fec657d0#subdirectory=packages/kitt-assistant-runtime"
      - name: Regression suite
        run: python -m unittest discover -s tests -v
      - name: Pytest suite
        run: python -m pytest -q
      - name: Compile package
        run: python -m compileall -q kitt
      - name: Static critical checks
        run: |
          python -m pip install ruff
          ruff check kitt --select F821,E9,B904,B905,S314
